name: Deploy

on:
  push:
    branches: [develop, main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'production' || 'develop' }}
    concurrency:
      group: deploy-${{ github.ref_name == 'main' && 'production' || 'develop' }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
      - name: Deploy plugin via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          port: ${{ secrets.SFTP_PORT }}
          password: ${{ secrets.SFTP_PASSWORD }}
          key: ${{ secrets.SFTP_KEY }}
          source: "./*"
          target: ${{ secrets.PLUGIN_TARGET_DIR }}
          overwrite: true

      - name: Clean up deleted files
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          port: ${{ secrets.SFTP_PORT }}
          password: ${{ secrets.SFTP_PASSWORD }}
          key: ${{ secrets.SFTP_KEY }}
          script: |
            # Remove deleted Migration features
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/src/ZeroSense/Features/WooCommerce/Migration/OrderValidationPage.php" 2>/dev/null || true
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/src/ZeroSense/Features/WooCommerce/Migration/MetaBoxMigrator.php" 2>/dev/null || true
            # Remove Migration directory if empty
            rmdir "${{ secrets.PLUGIN_TARGET_DIR }}/src/ZeroSense/Features/WooCommerce/Migration" 2>/dev/null || true
            
            # Remove deleted Service Location column feature
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/src/ZeroSense/Features/WooCommerce/OrderManagement/AdminOrderServiceLocation.php" 2>/dev/null || true
            
            # Remove debug scripts
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/debug-validation-page.php" 2>/dev/null || true
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/check-validation.php" 2>/dev/null || true
            rm -f "${{ secrets.PLUGIN_TARGET_DIR }}/force-delete-validation.php" 2>/dev/null || true
            
            echo "Cleanup completed"

      - name: Clear caches post-deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          port: ${{ secrets.SFTP_PORT }}
          password: ${{ secrets.SFTP_PASSWORD }}
          key: ${{ secrets.SFTP_KEY }}
          script: |
            # Clear PHP opcache if available
            if command -v php > /dev/null 2>&1; then
              php -r "if(function_exists('opcache_reset')){opcache_reset();echo 'opcache cleared';}else{echo 'no opcache';}" 2>/dev/null || true
            fi
            # Clear ZS feature transients via WP-CLI if available
            if command -v wp > /dev/null 2>&1; then
              wp transient delete --all --path=$(dirname $(dirname $(dirname "${{ secrets.PLUGIN_TARGET_DIR }}"))) 2>/dev/null || true
            fi
